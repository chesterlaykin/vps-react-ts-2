// import {  } from '@/fffff';
// import { ffff } from '@/types/fff';
// import s from './fffff.module.scss';
type IProps = {
    Fffff: string;
};
const initialValues = {
    thing: 'dd'
}
export const Page = ( pr : IProps) => {

    pr = { ...initialValues, ...pr }
    

    return (
        <div></div>
    );
};